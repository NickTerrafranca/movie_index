<h2>Movies</h2>
  <ul>
    <% @movies.each do |movie| %>
      <li>
        <a href="<%="/movies/#{movie[1]}" %>">
        <%= "#{movie[0]} ~" %>
        </a>
      </li>
    <% end %>
  </ul>

<!-- </body> -->
